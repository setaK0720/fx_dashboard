# 開発ログ

## 2025-11-29: 注文履歴機能の実装とUI改善

### 作業内容
1. **注文履歴機能の実装**
   - バックエンド: `MT5Client` に `get_history_deals` を追加し、`/api/history` エンドポイントを実装。
   - フロントエンド: `HistoryPanel` コンポーネントを作成し、サイドバーとルーティングに追加。
   - 修正: 当日の注文が表示されない問題を、取得期間の終了日時を翌日に設定することで解決。

2. **UI改善**
   - `PositionTable` のCloseボタンの確認ダイアログを削除。

### 変更理由
- ユーザーより「過去の注文履歴を見たい」との要望があったため。
- 「Closeするときに確認ダイアログを表示しないようにして」との要望があったため。

### 結果
- 履歴画面で過去の取引が確認できるようになった。
- ポジション決済がワンクリックで行えるようになった。
