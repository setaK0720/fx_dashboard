# 開発ログ

## 2025-11-24: ドロップダウンリストの表示修正

### 作業内容
- `RatePanel` のドロップダウンリストが `AccountInfoWidget` に隠れる問題を修正。
- `RatePanel.tsx` に `zIndex={10}` と `position="relative"` を追加。

### 変更理由
- `backdrop-filter` を使用している要素同士の重なり順（スタッキングコンテキスト）の問題により、ドロップダウンが背面に描画されていたため。

### 結果
- ドロップダウンリストが正しく前面に表示されるようになった（はず）。
