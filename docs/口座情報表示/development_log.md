# 開発ログ - 口座情報の表示

## 2025-11-24

### 口座情報の表示機能実装
- **作業内容**:
    - バックエンド: `MT5Client` に `get_account_info` メソッドを追加。`main.py` に `/ws/account` WebSocketエンドポイントとブロードキャストタスクを追加。
    - フロントエンド: `useAccountInfo` フックと `AccountInfoWidget` コンポーネントを作成。`App.tsx` にウィジェットを追加。
    - UI調整: 項目名を日本語化（残高、有効証拠金など）。

- **変更理由**:
    - ユーザーがダッシュボード上でリアルタイムに口座状況を確認できるようにするため。

- **結果**:
    - ダッシュボードに口座情報が表示され、リアルタイムに更新されることを確認。
